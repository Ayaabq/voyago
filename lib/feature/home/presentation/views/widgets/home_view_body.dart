import 'package:flutter/material.dart';

class HmoeViewBody extends StatelessWidget {
  const HmoeViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
